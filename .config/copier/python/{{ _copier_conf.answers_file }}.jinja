# Written by copier; `uvx copier update --trust` reads it. Do not edit by hand.
# _src_path is this machine's dotfiles checkout — rewrite it before publishing
# the repo if the path should not be public.
{{ _copier_answers|to_nice_yaml -}}
