"""Starter test, so `mise run check` passes on a fresh project.

pytest exits 5 — "no tests collected" — on an empty suite, which fails the gate
before any code exists. Replace this with a real test rather than deleting it.
"""


def test_smoke() -> None:
    assert True
